{
  "$schema": "../gen/schemas/macOS-schema.json",
  "identifier": "default",
  "description": "Default capabilities for CloudNest",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
